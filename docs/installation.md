# Installation

## Home Assistant

Install through HACS as an Integration, or manually copy `custom_components/wehere_hybrid` into the Home Assistant configuration directory.

After restart, add **WeHere Hybrid Lock** from **Settings → Devices & services**.

## ESP32

Use `esp32/airbnk.example.yaml` as the starting point for the companion gateway.
